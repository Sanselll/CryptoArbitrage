# Minimal requirements for ML API server (production)
# Only includes packages needed for RL inference

# Web Framework
flask>=3.0.0
flask-cors>=4.0.0

# Core ML Libraries
numpy>=1.24.0,<2.0.0
pandas>=2.0.0,<3.0.0
scikit-learn>=1.3.0,<2.0.0

# RL Libraries
stable-baselines3>=2.3.0
gymnasium>=0.29.0
torch>=2.0.0

# Data Processing
pyarrow>=13.0.0  # For price history loading

# Model Loading
joblib>=1.3.0
